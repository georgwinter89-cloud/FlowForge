node_modules/
out/
dist/
# Wegwerf-Fläche der Agenten (SPEC §3.3) — wird am Lauf-Ende geleert und gehört
# nie in einen Commit. Ohne diese Zeile nähme ein `git add -A` die Messdateien
# der Prüfer mit; gemessen: `git check-ignore` fand vorher keinen Treffer.
arbeitsablage/
